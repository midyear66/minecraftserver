# Admin panel credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# Absolute path to mc_data on the host (used by docker-compose)
HOST_DATA_DIR=./mc_data

# Docker group ID for socket access (auto-detect with: getent group docker | cut -d: -f3)
DOCKER_GID=
