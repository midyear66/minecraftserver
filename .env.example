# Admin panel credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# Absolute path to mc_data on the host (required for Docker API volume mounts)
# Run ./setup.sh to auto-detect, or set manually: /full/path/to/mc_data
HOST_DATA_DIR=

# Docker group ID for socket access (auto-detect with: getent group docker | cut -d: -f3)
DOCKER_GID=
