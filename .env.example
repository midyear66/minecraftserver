# Admin panel credentials (set by ./start_mcserver on first run)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# Absolute path to mc_data on the host (auto-detected by ./start_mcserver)
HOST_DATA_DIR=

# Docker group ID for socket access (auto-detected by ./start_mcserver)
DOCKER_GID=
